---
status: active
---

# Boot Matrix

Task type → required reading. Load only what's relevant, not everything.

| Task Type | Required Reading | Follow-up Reading | Code Anchor | Do NOT Treat as Current |
|-----------|-------------------|--------------------|--------------|--------------------------|
| FK maths | `docs/FR5_DH_Table.md` | `GLOSSARY.md` §1 | `geometry_backend.py` | — |
| Mesh rendering | `docs/FR5_Mesh_Convention.md` ★ | `docs/Polyscope_Quickstart.md`, `GLOSSARY.md` §2 | `geometry_backend.py`, `gui_panel.py` | — |
| Tool head / TCP | `docs/FR5_Mesh_Convention.md` (Nozzle and TCP section) | `GLOSSARY.md` §3 | `assets/printerHead/TCP.txt` | — |
| IK | `docs/FR5_DH_Table.md`, `docs/FR5_Joint_Limits.md`, `docs/FR5_IK_Derivation.md` | `GLOSSARY.md` §1 | `geometry_backend.py` (`solve_ik`, `solve_ik_tcp`) | Anything describing IK as "not started" or "no IK code exists yet" |
| UI / sliders | `docs/Polyscope_Quickstart.md` (ImGui widgets section) | `docs/FR5_Joint_Limits.md` (slider ranges) | `gui_panel.py` | — |
| Build plate / G-code | `wiki/003_Guides/BuildPlate_UserFrame.md`, `wiki/003_Guides/Gcode_Toolpath.md` | `GLOSSARY.md` §3, `settled.md` S1.2/S1.3/S1.6/S1.7/S1.8/S1.23 | `geometry_backend.py` (`load_build_plate`, `save_build_plate_position`, `load_saved_build_plate_position`, `parse_gcode`, `load_gcode`, `clear_gcode_preview`), `gui_panel.py` ("Build Plate Orientation" panel) | Anything describing the build plate as translation-only, or the G-code toolpath as auto-reloading/staying in sync when the plate moves (removed by S1.23 — an explicit "Load G-code preview" click is required) |
| Toolpath execution / IK precompute / playback / caching | `settled.md` S1.14–S1.24 | `GLOSSARY.md` §5 | `geometry_backend.py` (`run_/step_/pause_/cancel_toolpath_ik_precompute`, `save_/load_toolpath_precompute_cache`, `run_/pause_/reset_/advance_toolpath_playback`, `_reset_toolpath_playback_state`), `gui_panel.py` ("Toolpath Settings" panel) | Anything describing toolpath execution as not started, precompute/playback as immune to the plate moving mid-session, or `cancel_toolpath_ik_precompute()` as safe to call without resetting playback (fixed in S1.24) |
| Curved model (Stage 6.1) | `wiki/003_Guides/CurvedModel_Loading.md` | `settled.md` S1.29/S1.33, `tutorials/Stage6_README.md`, `wiki/001_Inbox/2026-07-18_curved_surface_assets.md` | `geometry_backend.py` (`read_ply_polyline`, `reconstruct_polylines`, `transform_points`, `_register_curve_layer`, `load_curved_model` — all generic), `examples/curved_surface_printing/study_config.py` (`CURVED_LAYERS`, `CURVED_MODEL_DIR`, `CURVED_MODEL_ROTATE_X_DEG`, `CURVED_OBSTACLE_*` — the RX/TX-specific config), `gui_panel.py` ("Load Curved Model" button, "I/O Operations" panel) | Anything describing curved-model placement as translation-only (S1.29's amendment added a fixed 90° rotation about local X), polyline reconstruction as a plain "walk from a degree-1 endpoint" (misses 6 of the 70 pieces, which are closed loops with no degree-1 node), or RX/TX file lists as hardcoded in `geometry_backend.py` (moved to `study_config.py` by S1.33) |

| Curved-model geodesics (Stage 6.2) | `wiki/003_Guides/CurvedModel_Geodesics.md` | `settled.md` S1.31/S1.29/S1.30/S1.33, `tutorials/Stage6_README.md` | `geometry_backend.py` (`build_surface_graph`, `nearest_vertex_index`, `dijkstra_surface`, `geodesic_path_nodes`, `_layer_endpoints_world`, `run_/step_/pause_/cancel_/_abort_geodesic_precompute` — all generic, iterate over however many layers `CURVED_LAYERS` describes), `gui_panel.py` ("Build Geodesics" button, RX/TX layer selector driven by `content.curved_layer_names`, "I/O Operations" panel) | Anything describing `load_curved_model()` as retaining no state (6.2 makes it retain world-space curve and surface arrays); the print surfaces as disconnected because they aren't watertight (each is exactly **one** connected component — measured, zero unreachable pairs, so don't build component-handling machinery); the cost matrices as needing 140 Dijkstra runs (it's 113 — one per *unique snapped vertex*, not per endpoint); `GEODESIC_LAYER_RX`/`GEODESIC_LAYER_TX`/`GEODESIC_LAYER_NAMES` as still existing (removed by S1.33); or the `show_sample_geodesic` sample aid + its `_isolate_/_restore_geodesic_isolation` helpers + sample-only `GEODESIC_CURVE_/CHORD_/HOST_*` constants as still existing (removed 2026-07-21, S1.31 — superseded by 6.3's overlay + `apply_live_layer_visibility`). Also: any zero-cost pair count quoted without saying whether it means matrix *entries* or endpoint *pairs* — the matrix is symmetric, so 8 RX pairs are 16 entries |

| Curved-surface print ordering / layers & passes (Stage 6.3+) | `wiki/003_Guides/CurvedModel_PrintOrder.md`, `settled.md` S1.32/S1.33/**S1.35** | `tutorials/Stage6_README.md` ("One live layer at a time", 6.3), `settled.md` S1.30/S1.31, `GLOSSARY.md` §7, `wiki/003_Guides/CurvedModel_Geodesics.md` | `geometry_backend.py` (`greedy_piece_order`/`two_opt`/`travel_cost`/`compute_vertex_normals` — module-level; `build_print_order`/`apply_live_layer_visibility`/`_sequence_colors`/`_orient_normals_outward`/`_surface_boundary_vertices`/`_layer_endpoints_world`, the geodesic cost matrices — generic; `CURVED_TRAVEL_HOVER_MM`/`CURVED_TRAVEL_COLOR`/`CURVED_ORDER_CMAP`/`CURVED_ORDER_FEED_RADIUS_MM` constants), `examples/curved_surface_printing/study_config.py` (`CURVED_LAYERS` list order encodes RX-first; future per-pass collision-obstacle/fill-messaging logic belongs here, per S1.33), `gui_panel.py` ("Build Print Order" button, layer selector driven by `content.curved_layer_names`) | Anything saying **TX prints first** (superseded by S1.32 — **RX** is the first pass) or that the **print order is an open question** (answered 2026-07-20) or **not yet implemented** (6.3 done — S1.35); anything merging the two layers into one toolpath, one cost matrix, one cache file or one playback, or emitting a travel move between the last RX piece and the first TX piece (a manual silicone fill sits in that gap); treating the per-layer visibility rule as cosmetic (RX is sealed inside the TX shell and is *invisible* with `Surface TX Base` shown); using `Surface_Bot` as the collision obstacle for **both** passes (the TX pass must clear `Surface_RX_Offset`); adding new bare RX/TX-named constants to `geometry_backend.py` instead of `study_config.py` (S1.33 — but generic job-tuning like hover/travel-colour *does* stay in `geometry_backend.py`); or describing surface normals as coming from trimesh's `vertex_normals` (needs scipy, degrades to ~84% outward — replaced by from-scratch `compute_vertex_normals`, S1.35) |

| Curved-surface tool orientation (Stage 6.4) | `wiki/003_Guides/CurvedModel_Orientation.md`, `settled.md` **S1.36** | `tutorials/Stage6_README.md` (6.4), `settled.md` S1.12 (the flat-plate assumption it supersedes), S1.35 (the outward normals it reuses), S1.4/S1.5 (TCP `R_target` convention + reference-pose ranking 6.5 will use) | `geometry_backend.py` (`build_orientation_frames`, `_register_orientation_frames`, `apply_live_layer_visibility`; `ORIENT_FRAME_SCALE_MM`/`ORIENT_FRAME_STRIDE`/`ORIENT_FRAME_COLORS` constants; `curved_orient_frames`/`curved_orient_loaded` state), `gui_panel.py` ("Build Orientation Frames" button) | Anything describing the curved path as using **one constant `R_target`** (S1.12 — superseded per-waypoint by S1.36); the nozzle-spin DOF as **aligned to the path tangent** (the Stage6_README's first guess — it's actually pinned to a fixed world reference for arm stability, since the nozzle is rotationally symmetric); 6.4 as **solving IK** or driving the arm (it only computes + visualises orientation frames — IK wiring is 6.5); or surface normals as needing a fresh sign check (already fixed outward against `Surface_Bot` at load, S1.35) |

| Curved-surface IK precompute (Stage 6.5) | `wiki/003_Guides/CurvedModel_IKPrecompute.md`, `settled.md` **S1.37** | `tutorials/Stage6_README.md` (6.5), `settled.md` S1.36 (the orientation frames it consumes), S1.12/S1.13/S1.21 (the Stage-5 chunked-precompute/ground-clearance/cache machinery it reuses rather than rewrites) | `geometry_backend.py` (`_begin_toolpath_precompute` — the shared seam both `run_toolpath_ik_precompute` (planar) and `run_curved_toolpath_ik_precompute(layer, ...)` (curved) feed; `build_curved_toolpath_waypoints_world`, `_orientation_frames_for_points`, `_nozzle_clears_plane`, `_branch_clears_ground` (now takes an optional `plane` arg), `curved_precompute_cache_path`, `_curved_toolpath_cache_meta`; `CURVED_TIP_CLEARANCE_TOLERANCE_MM` constant; `precompute_cache_path`/`precompute_tip_tolerance_mm` state) | Anything describing curved nozzle clearance as checked against a **per-pass obstacle mesh** (`Surface_Bot`/`Surface_RX_Offset` — the inbox note's original suggestion, rejected as too slow; replaced by a per-waypoint tangent-plane supporting-hyperplane check, S1.37); the **arm links** as tested for clearance (only the nozzle tip is — testing the links would reject every real printing pose, since they legitimately sit inward of a local tangent plane); the **world `z=0` ground check** as permanently dropped for curved / permanently always-on for planar (6.5 dropped it for curved, but **6.6 made it the toggleable `reject_below_ground`, default ON, layered on the tangent-plane check for both paths — S1.38**); one shared precompute cache file across planar/RX/TX (each pass gets its own, `curved_<layer>.precompute.npz`); `PRECOMPUTE_CACHE_VERSION` as **2** (bumped to **3** by 6.6's toggle, S1.38); or 6.5 as wiring up a GUI button or curved playback (that's 6.6 — scope of 6.5 itself is `geometry_backend.py` only) |

| Curved GUI wiring / toolpath source / z=0 toggle (Stage 6.6) | `settled.md` **S1.38**, `tutorials/Stage6_README.md` (6.6) | `wiki/003_Guides/CurvedModel_IKPrecompute.md` (the z=0 toggle's clearance role), `settled.md` S1.35 (the strict-isolation visibility it replaces with the S1.32 stack), S1.13 (the planar z=0 check it makes toggleable) | `geometry_backend.py` (`toolpath_source`/`reject_below_ground` state; `run_active_toolpath_ik_precompute`, source-aware `run_/reset_/advance_toolpath_playback`, `_build_curved_beads`, `_init_curved_toolpath_playback`, `clear_curved_model`, `curved_model_summary`, `_clear_gcode_print_mesh`, `apply_live_layer_visibility` (now `i <= layer` stack rule), `_branch_clears_ground` (z=0 gated on `reject_below_ground`, layered with the tangent plane); `CURVED_BEAD_WIDTH_MM`/`CURVED_BEAD_HEIGHT_MM` constants; per-layer `curved_bead_*` state), `gui_panel.py` ("Toolpath Source" selector, Load/Clear Curved Model, "Curved Model Properties" dropdown, "Reject poses below ground (z<0)" checkbox) | Anything describing a **separate/duplicated curved precompute+playback control set** (there is ONE source-aware set, dispatched by `toolpath_source` — user directive: reuse, don't duplicate); the RX/TX **radio** as the layer control (replaced by the "Toolpath Source" selector incl. `Planar (G-code)`); `apply_live_layer_visibility` as **strict one-layer isolation** (now the S1.32 stack, `i <= layer` — TX shows the printed RX beneath); the **z=0 ground check** as curved-off/planar-always-on (now the `reject_below_ground` toggle, default ON, both paths, folded into the cache key); or switching the active source mid-run as safe without the layer-mixup guard (it force-cancels a mismatched paused run) |

★ = Getting this wrong renders the whole arm incorrectly — always read
before touching mesh transform code, per the warning in
`docs/FR5_Mesh_Convention.md` itself.
