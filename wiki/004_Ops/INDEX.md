---
status: active
---

# 004_Ops

Environment setup and deployment notes. This project is a local, offline
Polyscope app (no server/deploy target), so this is currently limited to
environment setup — see root `README.md` and `requirements.txt` for the
canonical install steps.
